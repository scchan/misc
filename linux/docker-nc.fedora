#!/bin/bash


if [[ "$#" != 2 ]]; then
  echo "Usage: docker-nc <image_name> <container_name>"
  exit
fi

IMAGE=$1
CONTAINER=$2

docker run --privileged -tid -e "container=docker" -v /sys/fs/cgroup:/sys/fs/cgroup --expose 22 -P --name $CONTAINER   $IMAGE /usr/sbin/init
docker port $CONTAINER


